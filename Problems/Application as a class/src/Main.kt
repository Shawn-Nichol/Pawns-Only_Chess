class Application(val name: String) {

    // write the run method here
    fun run(s1: String, s2: String, s3: String) {
        println(name)
        println(s1)
        println(s2)
        println(s3)
    }
}