class Rectangle() {
    var width: Int = 1
    var length: Int = 1

    constructor(width: Int, length: Int) {
        this.width = width
        this.length = length
    }
}

val rectangle = Rectangle()